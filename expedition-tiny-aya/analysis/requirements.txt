# Dependencies for data pipeline scripts
datasets>=2.14.0
langdetect>=1.0.9
huggingface_hub>=0.16.0

# Dependencies for visualization (plot_condition_comparison.py + phase-3 figures)
matplotlib>=3.8.0
seaborn>=0.13.0
pandas>=2.0.0
numpy>=1.24.0
scienceplots>=2.1.0  # Recommended for EMNLP-style typography on phase-3 figures; the fig scripts have a graceful fallback if absent, but the default analysis env installs it.
