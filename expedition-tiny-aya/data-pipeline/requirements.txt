# Dependencies for batch transpilation pipeline
legesher-core>=0.5.1,<1.0
legesher-i18n>=0.5.1,<1.0
tqdm>=4.60.0,<5
datasets>=2.14.0,<3      # Only needed for --hf-dataset streaming
huggingface_hub>=0.16.0,<1  # Only needed for --hf-dataset streaming
boto3>=1.34.0,<2         # Needed for The Stack v2 content fetches from Software Heritage S3
